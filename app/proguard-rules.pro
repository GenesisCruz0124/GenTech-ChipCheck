# Add project specific ProGuard rules here.
# minifyEnabled is currently disabled for release builds (see app/build.gradle.kts),
# so these rules are not exercised yet; kept as a starting point for when it's turned on.
